# ADK 改进建议与问题清单（2026-09-24，给下一次会话）

> 来源：用 ADK 把 Metronic demo1 仪表盘整页复刻出来的一次实战（项目 `E:\--gitrepro--\adk-metronic-demo`）。
> 本文件写给「优化 ADK 设计与代码」的新会话：先读 `CLAUDE.md`，再读本文件，再看 §6 的坐标。
> 每条判断都附证据（文件 / 命令 / 数字）；没有验证过的地方会标「未验」。

---

## 0. 开工前必读

1. **工作树里有 148 个未提交改动**，是 2026-07-08「静态页面验证」那一轮的产物：`systems/ant-design` 131 个文件（契约与 sources 快照）、`fixtures/*`、`index/*`、`reports/static-test-limitations.md`、`pencil/generated`、`generated-code`。这些是 Jovi 的工作成果，**先提交（或让 Jovi 决定），绝不能 checkout / stash 丢掉**。2026-09-24 之后唯一的新提交是 `64d9d13 react compiler`（已推送 origin/main）。
2. **Jovi 的规矩**：引擎 / 校验器 / 编译器 / 脚本的 bug 可以修；**契约文件 `systems/*/components/*.contract.json` 是编译产物，不要手改**。契约有缺口时走「重新 ingest 该组件」或纠错机制，让改动可追溯。（本次我曾手加 `layout.siderWidth` 并跑 apply-correction，按他要求全部回退。）
3. **环境**：Windows 11，Python 3.14.7；`jsonschema` 要 `pip install jsonschema`（仓库没有依赖声明）；控制台是 GBK，跑引擎前 `set PYTHONIOENCODING=utf-8`，否则 print 中文 / 箭头会崩；Node 22，本机有 Chrome。本机 Claude Code 的子代理起不来（用户全局 settings 把子代理模型钉到不存在的 glm-5.3），所有事自己做。
4. **回归基线 = demo 项目**：

   ```bash
   cd E:\--gitrepro--\adk-metronic-demo
   python adk/build.py          # 期望：86 ops → 155 nodes，19 种组件；passed=True，blocking 0 / risky 1 / improvement 0
   npm run build && npm run shot  # shots/metronic-demo1-full.png 应与当前一致
   ```

   另外五个 fixture（user-permissions-page、shadcn-login-page、notification-settings-page、dashboard-asset-management、data-comparison-analysis）用新编译器都能 `compile-code --framework react`（本次已验，产物未做 tsc）。

---

## 1. 这次做了什么（上下文）

- **目标**：给 Jovi 作品集 /p23（AI Design Kit 案例页，`E:\--gitrepro--\uxwork\uxwork-vue-new\src\content\projects\23-ai-design-kit\index.md`）一张「真正用 ADK 做出来的页面」截图。参照页 https://keenthemes.com/metronic/tailwind/demo1/ ，整页参照图在 `E:\--gitrepro--\uxwork\_cover-gen\tmp\metronic\demo1-full.png`（1440 宽）与 `demo1-1920-full.png`。
- **流程严格按 ADK 协议**：`adk/intent.json` → `adk/author-ops.py` 展开设计计划为 `ops.json`（86 条，只用 `component.insert` / `component.fillSlot`）→ `adk/build.py` 调 `engine.ops.apply_ops` + `engine.validator.validate_graph` + `engine.compilers.react` → `graph.json` / `validation-report.json` / `src/generated/MetronicDemo1.tsx` / `tokens.json` + `tokens.css` → Vite + React 18 + antd 5.29 渲染 → playwright-core 驱动 Chrome 截图。页面里没有一处手写 HTML / CSS 去模仿组件。
- **页面内容**：Layout（Sider 280 + Header + Content + Footer）；顶栏一个水平 Menu；侧栏分组 inline Menu（extra: Soon）；页头 Flex（Title + 次要 Text + 默认 Button）；第一行 4 张 KPI 卡（Avatar 图标 + Statistic）+ 引导卡（Space 头像 + Title + Paragraph + actions 链接按钮）；第二行 Highlights（Statistic + Tag + Progress success 分段 + Badge 图例 + Divider + 无表头小 Table）+ Earnings（Switch + Select + 12 行 Row/Col + Progress）；第三行 Team Meeting（Descriptions + actions）+ Teams（Input 搜索 + Table：rowSelection、sorter、pagination）；页脚 Text。19 种组件：avatar、badge、button、card、descriptions、divider、flex、grid、input、layout、menu、progress、select、space、statistic、switch、table、tag、typography。决策与取舍表在 `adk-metronic-demo/adk/rationale.md`。
- **改了 ADK 的什么**：只有 `engine/compilers/react.py`（commit `64d9d13`）。改前它把数组 / 对象属性输出成 Python repr 字符串（`items="[{{'key': ...}}]"`），Layout / Grid / Card 的插槽全部拍平成兄弟节点，产物不可运行。改后：非标量属性 → JS 字面量；icon / prefix / suffix / extra 等位置上的 `XxxOutlined|Filled|TwoTone` 字符串 → `@ant-design/icons` 的真实 import；`antd.layout` 插槽 → `Layout.Header / Sider / Content / Footer`（有 sider 时嵌内层 Layout，sider* props 落到 Sider）；`antd.grid` 带列属性 → `Col`，否则 `Row`；`antd.card` 的 header / cover / actions 插槽 → `title / cover / actions` prop；`antd.typography` 按 variant → `Typography.Title / Text / Paragraph`；契约 boolean 类型的 'true' / 'false' 字符串收敛为布尔；`button intent=danger` → `danger`；新增 `resolved_tokens()` 供宿主生成 tokens.css / ConfigProvider 主题。
- **宿主只做两件事**（`src/main.tsx`、`src/app.css`）：ConfigProvider 主题的值全部取自 token 图；页高、间距、细线、栅格内卡片等高用生成的 token CSS 变量写。
- **一条 risky 是故意留的**：Layout 契约没有 Sider 宽度属性，antd 默认 200px 会截断「Store - Services · Soon」，语义图里写了 `siderWidth: 280`，校验器报 `prop 'siderWidth' not declared on antd.layout`。这正是契约缺口的诚实呈现，作品集页面上也是这样写的。

---

## 2. 我对 ADK 的理解（给新会话的地图）

**一句话**：ADK 是一台编译器加一道闸门。编译器把设计规范源（官网 / Figma / 代码 / Storybook）编译成组件契约 + Token 图 + 组合语法（`systems/<sys>/`）；闸门是校验器，AI 只能通过九种语义操作改一张组件图，图经校验后由确定性编译器输出 Pencil / React / HTML。语义图是唯一真相源，各编译目标互不反推。

**文件地图（截至 2026-09-24）**

| 位置 | 内容 | 我看到的状态 |
|---|---|---|
| `kit.json` | 体系注册（ant-design、shadcn）、成熟度 Level 2 | 正常 |
| `systems/ant-design/components/*.contract.json` | 64 个契约：meaning / variants / props / slots / tokenBindings / hardRules / softRules / examples / code / pencil / sourceEvidence | 内容丰富，但见 §3.1、§3.4 |
| `systems/ant-design/tokens/token-graph.json` | 50 个 token，四层 + 别名 + light/dark | 够用但偏薄（没有字号 / 间距梯度，组件 token 很少） |
| `systems/ant-design/compositions/*.pattern.json` | 3 个组合模式 | 只有后台管理类 |
| `systems/ant-design/mappings/kit-to-react.json` | 组件 → React 名与 propMap | 与契约 `code` 段重复 |
| `systems/ant-design/sources/*.snapshot.json` | 每组件的抓取事实 | 契约的上游，re-ingest 的起点 |
| `shared/ontology/intent-taxonomy.json` | `concepts`（意图本体） | 检索按 concept id 精确匹配，见 §3.5 |
| `shared/rules/*.json` | 全局硬规则 8 条、反模式 8 条、a11y、品牌 | 硬规则由 validator 的固定逻辑实现 |
| `schemas/*.schema.json` | 10 个 schema | `semantic-ops` 的 nodeSpec 不能带 slots（§3.6） |
| `engine/` | kit / contract / tokens / graph / ops / validator / assemble / retriever / indexer / feedback / release / compilers(html, pencil, react, pencil_mcp) / cli | Python，无第三方依赖除 jsonschema |
| `engine/cli.py` | validate-contracts / validate-tokens / validate-graph / validate-ops / compile-pencil / compile-library / compile-code / index / retrieve / apply-correction / release / export-kc / pencil-verify / compile-context / write-contract / write-tokens / write-system / ingest-snapshot / compile-report | 没有「ops → graph.json」的命令（fixture 的 graph 是脚本另生成的；demo 的 build.py 自己调 apply_ops） |
| `index/component-cards.json`、`intent-index.json` | 67 张卡（L0 一句话 / L1 摘要）+ 意图路由表 | 文件 UTF-8，GBK 控制台看是乱码 |
| `fixtures/` | 8 个页面 fixture + violations 样本 | 7 月产物大多未提交 |
| `memory/corrections.jsonl` | 1 条纠错（input placeholder ≤ 50） | 机制在，能力窄（§3.8） |
| `.claude/skills/adk-*` | 13 个编排 skill | 本次未用（子代理不可用），流程按 skill 文字执行 |
| `scripts/verify.py` | 8 项端到端检查 | Windows 下崩（§3.9） |

**校验器实际执行的东西**（`engine/validator.py`，这是理解 ADK 的关键）：
1. 组件已注册；2. 变体轴存在且取值合法；3. prop：未声明 → risky；enum / maxLength / minimum / maximum / required / requiredWhen（极简求值）；4. token 引用存在；顶层 prop 值形如 `#hex` / `rgb` → blocking；5. 插槽 accepts / min / max；6. 命名规则函数只有 3 个：primary 每组最多 1、破坏性操作需 Modal、图标按钮需 ariaLabel。**其余一切契约里写的 hardRules 都不执行**（§3.1）。`passed` = blocking 为 0。

---

## 3. 发现的问题（按严重度）

### 3.1 校验器只执行 6 / 353 条契约硬规则

- 数据（`python` 遍历所有契约）：hardRules 共 353 条 —— schema 282、graph 35、slot 21、token 9、fn 6。validator 只跑 `kind == "fn"` 且在 `RULE_FNS` 注册的（3 个函数、6 处引用）。其余的 `expr`（如 `props.items.required == true`、`items_have_unique_keys`、`props.percent between 0 and 100`、`props.tabList != null ? props.activeTabKey != null ...`）从不求值，也不报 unresolved（只有 fn 类未注册才记 risky）。
- 后果：契约里的大部分约束是散文。例：menu `HR-ITEMKEY-UNIQUE`、button `HR-PRIMARY-SIBLING` / `HR-DANGER-COHERENCE`、progress `HR-PERCENT-BOUNDS`、card `HR-TABLIST-CONSISTENCY` 都不拦；840 个 prop 里只有 30 个带 `required` 标记，所以 statistic 的 `HR-VALUE-REQUIRED` 也是靠 `required: true` 才生效。
- 建议：
  - A. 能结构化的转结构化：把 282 条 schema 类规则中「required / enum / min / max」的部分回填到 `props.*`（validator 已支持），规则本身保留为 message 来源；
  - B. 为 `expr` 做一个极小的安全表达式求值器（只支持 `props.x`、`slots.x.count`、比较、`in [...]`、`and / or / ? :`、`is_object` 等），解析失败记 `unresolved`（risky），把盲区显性化；
  - C. 常用图级规则补成 fn：items key 唯一、percent 范围、tabList 需 activeKey、Space/Flex 至少 1 项（min 已有）、Divider vertical 不带 title。
  - 验收：跑 `fixtures/violations/*` 应有对应 blocking；demo 与 5 个 fixture 仍通过。

### 3.2 硬编码颜色只扫顶层属性

- `_is_hardcoded_color` 只作用于 `node.props` 的直接值；嵌套值（`styles.content.color`、`columns[].onCell`、`items[].style`、`strokeColor` 数组）放过。demo 里 Tag `color="success"`、Badge `color="green"` 是预设名，能过是因为不是 hex，不是因为被识别为预设。
- 建议：递归扫描所有字符串值（跳过 key / label / title 等文本键），并为 Tag / Badge / Progress 的 `color` 类 prop 建预设名白名单（antd preset colors）。

### 3.3 编译器里还有组件特例表（违反「规则必须落进契约」）

- `engine/compilers/react.py` 的 `_LAYOUT_SLOTS` / `_SIDER_PROPS` / `_COL_PROPS` / `_CARD_SLOT_PROPS` / `_TYPOGRAPHY` / `_ICON_KEYS` 是编译器私有知识，换一套体系（shadcn）就要再写一套。
- 建议扩展契约 `code` 段（同步改 `schemas/component-contract.schema.json`）：`code.slotMap`（slot → 子组件标签 / prop 名 / children）、`code.variantMap`（变体值 → 子组件或 prop 值，如 typography.variant=title → `Typography.Title`，button.intent=danger → `danger:true`）、`code.subComponents`、`code.propTypes`（布尔 / 数字收敛依据）。编译器只读契约。`mappings/kit-to-react.json` 与契约 `code` 段是两份重复数据，二选一（建议以契约为准，mapping 由 index 派生）。
- 图标约定（字符串图标名）也应落成契约级声明（§3.7）。

### 3.4 契约的事实错误与缺口（走 re-ingest，不手改）

- `card.size` 枚举是 `medium | small`，antd 是 `default | small`。产物 `npx tsc --noEmit` 报 7 处 TS2322，渲染无碍。修契约要同步修用 `medium` 的 fixture（dashboard-asset-management 等）。
- `layout` 没有 Sider 的 `width`（也没有 collapsedWidth / trigger / zeroWidthTriggerStyle）。`layout.header` 只收 1 个 menu / typography / breadcrumb，`footer` 只收 typography；`card.header` 只收 typography / tag / button / breadcrumb（放不下 Input），`card.extra`、`statistic.title / prefix / suffix`、`descriptions.title` 这类 ReactNode prop 在契约里是 `componentRef`，语义图只能填字符串。真实产品头部（logo + 导航 + 用户菜单）与卡片头部的搜索框都无法表达。
  - 建议：结构性插槽放宽到接受 `antd.flex` / `antd.space` 容器；`componentRef` 类 prop 允许填 nodeSpec（或改造成 slot）。
- `statistic.value` 标 `number`，antd 接受 `string | number`（demo 用 "9.3k" 通过，只因 validator 不查类型）。要么类型改 `number | string`，要么 validator 开始查基本类型（那会暴露更多契约类型错误，先做 §3.4 末尾的核对再开）。
- 建议一个**自动核对**：Node 侧脚本读 `node_modules/antd/es/<comp>/index.d.ts`（或用 ts-morph）提取 props 名与字面量联合类型，与契约 `props` / `variants` 比对，产出 `reports/contract-vs-antd.json`。这类错误（medium）会自动浮出，之后每次 ingest 都跑。demo 项目里已有装好的 antd 5.29 可直接用。

### 3.5 检索（意图路由）只认本体里的 concept id

- `python -m engine.cli retrieve --intent dashboard` 与 `--intent 仪表盘` 返回 0 张卡、0 个契约；`--intent data_management` 返回 11 卡 + 2 模式，`--intent content` 返回 14 卡。`engine/assemble.py` 按 intent 精确匹配 `shared/ontology/intent-taxonomy.json` 的 `concepts`。自然语言意图进不来，「最小知识包」在真实任务里用不上——本次我是直接读契约文件做的。
- 建议：concepts 加中英同义词 / 关键词表；assemble 先分词 + 同义词映射再路由；命不中就按 category 退回全量卡并说明；返回结果附「命中原因」。向量检索仍留给 KC 接入。

### 3.6 语义操作的表达力与人体工学

- `semantic-ops.schema.json` 的 nodeSpec 不能带 `slots`，`fillSlot` 的 children 只有一层，所以每一层容器都要单独一条 op：一页 86 条。建议 nodeSpec 允许递归 `slots`，或增加 `component.insertTree`。
- 没有 remove / replace / move 类 op（`reorder` 只在同一容器内），迭代设计只能重建整图；`fillSlot` 是整槽替换而非追加，文档没写清。
- `contractRefs` 与 children 的 component 重复，可改为可选（由引擎推导）。
- `graph.project.name` 默认 `Untitled`，所有 fixture 的 React 组件都叫 `Untitled`；`compile-code` 应从 `-o` 文件名或 slug 推导。
- 没有「ops → graph.json」的 CLI 子命令；`validate-ops` 内部构了图却不落盘。建议 `build-graph <ops.json> -o graph.json`（demo 的 `adk/build.py` 可以直接搬进 cli）。

### 3.7 未建模的东西（要么建模，要么写进文档当边界）

- **图标**：不是组件。编译器现在的约定是「icon / prefix / suffix / extra / closeIcon / expandIcon / indicator 位置上的 `XxxOutlined|Filled|TwoTone` 字符串」。建议编译一份 `systems/ant-design/icons.json`（从 `@ant-design/icons` 导出列表生成），validator 校验图标名存在，契约 `code` 段声明哪些 prop 接受图标。
- **图表**：无。Earnings 的折线图只能用 12 行 Progress 表达同一组数据。要么接 `@ant-design/plots` 做一族 chart 契约，要么在 README 明写「图表不在范围」。
- **表格单元格渲染**：`columns[].render` 是函数，JSON 表达不了；评分、头像组、状态 Tag 在单元格里只能变成文本（demo 的 "★ 5.0"）。建议声明式 cell renderer 枚举（`tag / badge / rate / avatar / link / progress`），由编译器生成 render。
- **Avatar.Group**、Menu item 的 ReactNode label、Card.Meta：同类问题。

### 3.8 纠错机制的能力边界

- `engine/feedback.py` 的 `apply_correction` 只能追加 hardRule / softRule / example / compositionRule / slotRule，和给 prop 加约束（maxLength / minLength / minimum / maximum / enum）；不能增删 prop、不能改 slot accepts、不能改 variant 值。所以「Layout 缺 width」这类缺口没有正路可走，只能手改（Jovi 不允许）。
- 建议：patch 增加 `propAdd / propRemove / slotAccepts / variantValues`，并提供 `python -m engine.cli reingest <sys> <component>`（当前 ingest 是整体系），让契约缺口走「重新编译单个组件」的正路；每次都写 `memory/corrections.jsonl`。

### 3.9 工程与仓库卫生

- `scripts/verify.py` 在 Windows 崩：`subprocess.run(..., text=True)` 用 GBK 解码子进程的 UTF-8 输出（`compile-pencil` 打印的「→」），reader 线程 `UnicodeDecodeError`，`p.stdout` 变 None，随后 `p.stdout + p.stderr` 抛 TypeError。修法：`encoding="utf-8", errors="replace"`，子进程 env 加 `PYTHONIOENCODING=utf-8`。同文件第 1 项文案还写着「14 contracts」（现在 67）。
- 没有 `requirements.txt` / `pyproject.toml`：至少声明 `jsonschema`；建议 pyproject + `pip install -e .`，README「快速开始」补安装与 Windows 编码说明。
- 所有 CLI 打印建议 `sys.stdout.reconfigure(encoding="utf-8")`，避免 GBK 控制台崩。
- HTML 编译器只是结构镜像（`data-component` 的 div 树），没有可看的输出；要么删掉避免误解，要么改成真渲染（例如复用 React 产物做 SSR）。
- 148 个未提交文件（§0.1）。
- Pencil 编译器与 `pencil-verify` 本次未碰（未验）。

---

## 4. 建议的优先级

| 级别 | 内容 | 为什么先做 |
|---|---|---|
| P0 | §3.3 契约驱动的 `code` 段；§3.4 末尾的「契约 vs antd 类型」自动核对；把 demo + 5 个 fixture 的 React 产物纳入 `tsc` 检查 | 让产物「可交付、可验证」，这是 Level 2 → 实用的分水岭 |
| P1 | §3.1 硬规则真正执行；§3.2 颜色深扫 | 让「校验器」名副其实 |
| P2 | §3.8 纠错能增删 prop + 单组件 re-ingest；然后修 card.size、layout width、header / card.header 插槽 | 让契约能被修正而不手改 |
| P3 | §3.5 检索同义词路由 | 让「最小知识包」在真实任务里用得上 |
| P4 | §3.6 递归 nodeSpec / insertTree、build-graph 命令；§3.7 图标注册表、cell renderer、图表范围声明 | 人体工学与建模边界 |
| P5 | §3.9 卫生：verify.py、依赖声明、编码、HTML 编译器去留、README | 开源仓库第一眼 |

---

## 5. 验收

- `python scripts/verify.py` 全绿（修完 §3.9 后）。
- demo 回归：`python adk/build.py` 数字不变（86 / 155 / 19），截图与 `shots/` 一致；修完 card.size 后 `npx tsc --noEmit` 0 错。
- 5 个 fixture：`validate-ops` 通过；`compile-code --framework react` 产物过 tsc（可把产物放进 demo 项目的 `src/generated/` 借它的 antd 环境跑）。
- `fixtures/violations/*` 每个样本至少 1 条 blocking，并且 message 指向正确的规则。
- 「契约 vs antd 类型」报告 0 差异，或每条差异有解释。

---

## 6. 相关文件与坐标

- demo 项目：`E:\--gitrepro--\adk-metronic-demo`（`README.md` 命令；`adk/rationale.md` 组件决策表、缺口清单；`adk/ops.json` 是 ops 的真实样本；两个本地提交，无远程）。
- 作品集案例页：`E:\--gitrepro--\uxwork\uxwork-vue-new\src\content\projects\23-ai-design-kit\index.md`（对外口径：86 操作 / 155 节点 / 19 种组件、0 阻塞 1 风险；改 ADK 后若数字变了要同步）。
- 7 月的验证方案与结论：`ADK-静态页面验证方案-给CC.md`、`reports/static-test-limitations.md`（未提交）。
- 编译器提交：`64d9d13`（`engine/compilers/react.py`，模块 docstring 列了全部编译约定）。
- 参照页整页截图：`E:\--gitrepro--\uxwork\_cover-gen\tmp\metronic\demo1-full.png`、`demo1-1920-full.png`。

---

## 7. 一段总评（给决策用）

架构判断是对的：AI 只操作受约束的组件图，渲染与校验交给确定性引擎，这个位置主流生成工具没有占。约束层做实了一半——组件、变体、插槽、少数图级规则真的会拦；另一半（契约里 347 条不执行的规则、嵌套颜色、类型）还是散文。输出层最薄：编译器刚能出可运行的 React，且靠编译器私有特例；检索层在真实意图下命中率为零。把 P0 – P2 做完，它会从「能证明可控」变成「能直接交付」。
